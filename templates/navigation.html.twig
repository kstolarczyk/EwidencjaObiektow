{% trans_default_domain 'App' %}
<nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('obiekt_index') }}"><i class="fas fa-fw fa-book"></i> {{ 'Ewidencja.obiektow'|trans }}
    </a>
    <button class="btn btn-link btn-sm order-1 order-md-0" id="sidebarToggle">
        <i class="fas fa-bars"></i>
    </button><!-- Navbar Search-->

    <!-- Navbar-->
    <ul class="navbar-nav mr-0 ml-auto">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="userDropdown" href="javascript:void(0)" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user fa-fw"></i>
                <span class="d-none d-md-inline-block">{{ app.user.username }}</span>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                    <a class="dropdown-item" href="{{ path('user_index') }}"><i class="fas fa-fw fa-user-cog"></i> {{ 'Zarzadzanie'|trans }}
                    </a>
                {% endif %}
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-fw fa-sign-out-alt"></i> {{ 'Wyloguj'|trans }}
                </a>
            </div>
        </li>
    </ul>
</nav>